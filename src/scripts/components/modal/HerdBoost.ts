import Modal from "../../scenes/Modal/Modal";

export default class HerdBoost {
  public scene: Modal

  constructor(scene: Modal) {
    this.scene = scene
    this.create()
  }

  private create(): void {

    
  }

}